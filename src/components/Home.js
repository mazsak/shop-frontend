import React from 'react';
import NavBar from './NavBar';

const Home = (props) =>{
    return (
        <div>
            <nav>
                <NavBar/>
            </nav>
            <h1>Home</h1>
        </div>
    );
};

export default Home;