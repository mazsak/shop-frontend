import React from 'react';
import NavBar from './NavBar';

const Profile = (props) =>{
    return (
        <div>
            <nav>
                <NavBar/>
            </nav>
            <h1>Profile</h1>
            
        </div>
    );
};

export default Profile;
