import React from 'react';
import NavBar from './NavBar';

const Settings = (props) => {
    return (
        <div>
            <nav>
                <NavBar />
            </nav>
            <h1>Settings</h1>
        </div>
    );
};

export default Settings;

